"""Pipeline stages. Each is a pure function over the data model in model.py.

Stages depending on the built UMLS indexes (lookup) or on exact byte-parity
(tokenization) are specced here and implemented once the data build lands.
"""
